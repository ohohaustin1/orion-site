// PDF export stub
export const exportPDF = async (element: HTMLElement, filename: string) => {
  console.log('Export PDF:', filename);
};

export const exportAnalysisPDF = async (data: any, inputText?: string) => {
  console.log('Export analysis PDF:', { data, inputText });
};
