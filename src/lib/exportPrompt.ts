// Prompt export stub
export const exportPrompt = async (text: string, filename: string) => {
  console.log('Export prompt:', filename);
};

export const generateEngineeringPrompt = (data: any) => {
  return '';
};
